{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["sfo1"],
  "crons": [
    {
      "path": "/api/cron/warm-generation-v2",
      "schedule": "* * * * *"
    }
  ],
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "excludeFiles": "{docs/**,.github/**,benchmarks/**,tests/**,promo-video/**,out/**,output/**,input/**,.cache/**,.venv/**,_uv/**,.playwright-cli/**,.pytest_cache/**,build/**,*.egg-info/**,.DS_Store,**/__pycache__/**,**/*.pyc}"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.py"
    }
  ]
}
