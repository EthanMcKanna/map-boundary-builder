{
  "changed_areas": {
    "bay-area": {
      "status": "reference_mismatch",
      "note": "The Bay Area service area has changed since the saved screenshot/reference pairing, so fixtures in this area are tracked as data debt instead of scored as model accuracy."
    },
    "houston": {
      "status": "reference_mismatch",
      "note": "The Houston service area has changed since the saved screenshot/reference pairing, so fixtures in this area are tracked as data debt instead of scored as model accuracy."
    },
    "miami": {
      "status": "reference_mismatch",
      "note": "The Miami service area has changed since the saved screenshot/reference pairing, so fixtures in this area are tracked as data debt instead of scored as model accuracy."
    }
  },
  "fixtures": {
    "bay-area-tesla": {
      "status": "reference_mismatch",
      "note": "The Bay Area service area has changed since the saved screenshot/reference pairing, so this fixture is tracked as data debt instead of scored as model accuracy."
    },
    "bay-area-waymo": {
      "status": "reference_mismatch",
      "note": "The Bay Area service area has changed since the saved screenshot/reference pairing, so this fixture is tracked as data debt instead of scored as model accuracy."
    },
    "bay-area-zoox": {
      "status": "reference_mismatch",
      "note": "The Bay Area service area has changed since the saved screenshot/reference pairing, so this fixture is tracked as data debt instead of scored as model accuracy."
    },
    "houston-tesla": {
      "status": "reference_mismatch",
      "note": "The Houston service area has changed since the saved screenshot/reference pairing, so this fixture is tracked as data debt instead of scored as model accuracy."
    },
    "houston-waymo": {
      "status": "reference_mismatch",
      "note": "The Houston service area has changed since the saved screenshot/reference pairing, so this fixture is tracked as data debt instead of scored as model accuracy.",
      "current_image": "../h-waymo.png"
    },
    "las-vegas-zoox": {
      "status": "reference_mismatch",
      "note": "The local Zoox Las Vegas screenshot boundary and av-coverage-checker reference polygon are different shapes, so this pair is tracked as data debt instead of scored as model accuracy."
    },
    "miami-waymo": {
      "status": "reference_mismatch",
      "note": "The Miami service area has changed since the saved screenshot/reference pairing, so this fixture is tracked as data debt instead of scored as model accuracy.",
      "current_image": "../miami.png"
    }
  }
}
